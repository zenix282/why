{% extends 'base.html.twig' %}

{% block title %}Regel{% endblock %}

{% block body %}
    <h1>Regel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ regel.id }}</td>
            </tr>
            <tr>
                <th>Aantal</th>
                <td>{{ regel.aantal }}</td>
            </tr>
            <tr>
                <th>Factuur</th>
                <td>{{ regel.factuur }}</td>
            </tr>
            <tr>
                <th>Regel</th>
                <td>{{ regel.product }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('regel_index') }}">back to list</a>

    <a href="{{ path('regel_edit', {'id': regel.id}) }}">edit</a>

    {{ include('regel/_delete_form.html.twig') }}
{% endblock %}
